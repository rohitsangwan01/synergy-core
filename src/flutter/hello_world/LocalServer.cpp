#include "server/PrimaryClient.h"
#include "server/Server.h"

void startLocalServer() {
  //   PrimaryClient *primaryClient = nullptr;
  //   synergy::Screen *screen = nullptr;
  //   IEventQueue *events = nullptr;
  //   lib::synergy::ServerArgs args;
  //   Config config = Config();

  //   Server server(config, primaryClient, screen, events, args);

  
}